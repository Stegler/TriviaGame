$(documents).ready(function() {

    // Variables used for page
    var timeLeft = 180;
    var timer;
    var rightAnswers = 0;
    var wrongAnswers = 0;

    // The page will load the start button but will hide the time, questions, scoreboard, done button
    $"#timeClock").hide();
    
})